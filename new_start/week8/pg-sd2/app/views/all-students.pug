extends layout 
block content 
    - console.log(data);
    - console.log("confirmation it worked borhter");
    .main-content 
    h1 All Students
    table(border = 1)
        for item in data
            tr 
                td #{item.id} 
                td 
                    a(href="/single-student/" + item.id) #{item.name}
    p this is different bro